import { getOrder } from '@/lib/server-store';
import { isAdmin } from '@/lib/admin-auth';
function esc(v:unknown){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));}
function html(o:any){const rows=o.items.map((x:any)=>`<tr><td><b>${esc(x.name)}</b><div class="ar">${esc(x.ar)}</div><small>${esc(x.size)} · ${esc(x.milk)}</small></td><td>${Number(x.qty)}</td><td>SAR ${Number(x.unitPrice).toFixed(2)}</td><td>SAR ${Number(x.lineTotal).toFixed(2)}</td></tr>`).join('');return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${esc(o.id)}</title><style>@page{size:A4;margin:14mm}*{box-sizing:border-box}body{font-family:Tahoma,Arial,sans-serif;color:#1c1917;font-size:12px;line-height:1.55;max-width:900px;margin:auto;padding:24px}h1{text-align:center}.sub{text-align:center;font-size:16px;font-weight:700;margin-bottom:18px}.meta{direction:ltr;border:1px solid #ddd;border-radius:10px;padding:10px;margin-bottom:14px}table{width:100%;border-collapse:collapse;direction:ltr}th,td{padding:9px;border-bottom:1px solid #ddd;text-align:left}.ar{direction:rtl;text-align:right;font-weight:600}.totals{margin-top:16px;margin-left:auto;width:58%;border:1px solid #ddd;border-radius:10px;padding:10px}.line{display:flex;justify-content:space-between;padding:4px}.grand{font-size:15px;font-weight:800;border-top:1px solid #ccc;margin-top:5px;padding-top:8px}.actions{text-align:center;margin:18px}@media print{.actions{display:none}body{padding:0}}</style></head><body><div class="actions"><button onclick="window.print()">Print / Save as PDF · طباعة / حفظ PDF</button></div><h1>Maram's Cafe</h1><div class="sub">فاتورة ضريبية · Tax Invoice</div><div class="meta"><b>Invoice / الفاتورة:</b> ${esc(o.id)}<br><b>UUID:</b> ${esc(o.uuid)}<br><b>Issued:</b> ${esc(o.issuedAt)}</div><table><thead><tr><th>Item / الصنف</th><th>Qty</th><th>Unit</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><div class="totals"><div class="line"><span>Subtotal / قبل الضريبة</span><b>SAR ${Number(o.subtotal).toFixed(2)}</b></div><div class="line"><span>VAT 15%</span><b>SAR ${Number(o.vat).toFixed(2)}</b></div><div class="line grand"><span>Total / الإجمالي</span><b>SAR ${Number(o.total).toFixed(2)}</b></div></div></body></html>`;}
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,o:any=await getOrder(id);if(!o)return new Response('Not found',{status:404});const uuid=new URL(req.url).searchParams.get('uuid');if(!(await isAdmin())&&uuid!==o.uuid)return new Response('Unauthorized',{status:401});return new Response(html(o),{headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'private, no-store'}});}
